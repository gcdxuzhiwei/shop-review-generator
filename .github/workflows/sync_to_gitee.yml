name: Sync to Gitee

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Mirror the GitHub repo to Gitee
        uses: Yikun/gitee-mirror-action@master
        with:
          src: github/gcdxuzhiwei/shop-review-generator
          dst: gitee/suesxuzhiwei/shop-review-generator
          dst_key: ${{ secrets.GITEE_PASSWORD }}
          dst_token: ${{ secrets.GITEE_PASSWORD }}
          account_type: user
          force_update: true
